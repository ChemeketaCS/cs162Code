#include <iostream>

using namespace std;

void mystery(int n) {
    if (n > 20) {
        cout << "Done!" << endl;
    } else {
        cout << n << endl;
        mystery(2 * n + 1); //recursive call
    }
}

int main() {
    mystery(1);
}