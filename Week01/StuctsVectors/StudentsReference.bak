#include <iostream>
#include <vector>

using namespace std;

struct Student {
  string name;
  vector<double> exams;
};

int main() {
  // Make the students vector
  vector<Student> students;

  // Add two students to the vector
  Student student1 = {"Beth Jones", {84.2, 94.3, 96.7}};
  students.push_back(student1);

  Student student2 = {"John Smith", {78.2, 88.3, 92.7}};
  students.push_back(student2);

  // Clear exam scores for all students - flawed
  for (Student s : students) {
    s.exams.clear();
  }

  // Copy first student
  Student first = students.at(0);

  // Reference to first student
  Student& firstRef = students.at(0);

  // Clear exam scores via reference
  for (Student& s : students) {
    s.exams.clear();
  }

}
